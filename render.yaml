databases:
  - name: apteka-db
    plan: free
    postgresMajorVersion: "16"

services:
  - type: web
    name: apteka
    runtime: python
    plan: free
    buildCommand: "pip install -r requirements.txt && python manage.py collectstatic --noinput"
    startCommand: "python manage.py migrate --noinput && python manage.py bootstrap_admin && gunicorn config.wsgi --bind 0.0.0.0:$PORT"
    envVars:
      - key: ADMIN_BOOTSTRAP_USERNAME
        sync: false
      - key: ADMIN_BOOTSTRAP_PASSWORD
        sync: false
      - key: ADMIN_BOOTSTRAP_EMAIL
        sync: false
      - key: DATABASE_URL
        fromDatabase:
          name: apteka-db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: "False"
      - key: ALLOWED_HOSTS
        value: "*"
      - key: GEMINI_API_KEY
        sync: false
      - key: GEMINI_MODEL
        value: gemini-3.6-flash
      - key: CLOUDINARY_URL
        sync: false
      - key: ADMIN_ALERT_EMAIL
        sync: false
